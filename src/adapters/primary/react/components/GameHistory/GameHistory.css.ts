import { css } from "src/styles"
import { vars } from "src/styles/tokens.css"

export const gameHistory = css({
  listStyleType: "none",
  display: "flex",
  flexDirection: "column",
  gap: vars.space.xxxs,
})
